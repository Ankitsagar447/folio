{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Ankit Sagar Portfolio - Monorepo with Angular Client and Node/Express Server",
  "scripts": {
    "build": "npm run install:all && npm run build:client",
    "start": "node server/src/index.js",
    "build:client": "npm --prefix client run build",
    "start:client": "npm --prefix client start",
    "start:server": "npm --prefix server start",
    "dev:client": "npm --prefix client start",
    "dev:server": "npm --prefix server start",
    "install:all": "npm --prefix client install && npm --prefix server install"
  }
}
